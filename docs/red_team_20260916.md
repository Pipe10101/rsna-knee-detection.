# SlotKnee-S red-team self-review — consolidated (2026-09-16)

Scope: the RSNA 2026 knee-MRI entry as it stands on 2026-09-16 — public LB **0.905** (accuracy: two
16-epoch 5-fold sets, central + coverage, rank-averaged, TTA on) and **0.907** (efficiency: one full-fit
distilled student, TTA off); pooled OOF 0.8843; deadline 2026-10-22. Repository at `main = b280f22`
with 17 uncommitted files in the working tree. Nothing in the repository was modified, nothing was pushed
to Kaggle. Every critical/high finding was re-checked this pass by re-reading the cited lines and
re-running or re-reading the cited outputs; medium/low findings were graded from the saved evidence
under `redteam_20260916/C*/`.

---

## 1. Executive summary

**Numbers.** 65 findings raised across eight dimensions: **60 CONFIRMED, 5 PARTIAL, 0 REFUTED.**
Severity as raised: 5 critical, 17 high, 23 medium, 20 low. After consolidation one critical
(C8-docs-and-process-2) is re-graded high (the artefacts exist; only their tracking is missing), giving
4 critical / 18 high / 23 medium / 20 low. Several ids describe one defect seen from different
dimensions (worktree selection: C1-validation-2 = C4-code-1 = C8-docs-and-process-1; fold grouping:
C1-validation-1 = C4-code-3; `--cache` crash: C2-serve-skew-2 = C4-code-4; group embedding:
C2-serve-skew-1 = C4-code-5; LB-selection policy: C1-validation-6 = C7-experiment-design-5), so the
count of distinct problems is about 57.

**Nothing found invalidates the two scored entries.** The pixel path is bit-identical between cache and
inference, the deployed checkpoints are byte-identical to the local ones, the fold split reproduces
exactly, the large adopts (coverage, reg4, distillation, 16 epochs) are 7–14× any plausible noise, and
best epoch == last epoch on every fold so the OOFs carry no selection optimism.

**The five most important confirmed problems** (numbers first):

1. **The working tree would corrupt the next training and gate push** (C1-validation-2, C4-code-1,
   C4-code-2, C8-docs-and-process-1). Uncommitted `scripts/train_slotknee.py:1045-1047` selects the
   best epoch on `val_auc_gold` (≈12 gold studies per fold; `main` selects on `val_auc_derived`) and
   `scripts/compare_arms.py` now defaults to `gold_only=True` in `pool`, `pooled_macro`,
   `noise_from_seeds` and `per_fold_macro`. Replayed on the fold logs, gold selection would have picked
   epochs 13/10/12/15/14 instead of 16 for the central set (−0.0003…−0.0056 derived AUC per fold) and
   epoch 2 of 8 for `ssl_ep30` fold 0 (−0.09). No test pins the selection key; `package()` ships the
   working tree. Dormant today only because no OOF on disk carries `is_gold`.
2. **The 0.905 entry is no longer reproducible from the live inputs** (C5-kaggle-ops-1, -3, -4).
   `kaggle/slotknee_models/stack_weights.json` (mtime 20:04:38 today; checkpoints 09:33:19; scored
   submission 08:12 UTC) is in the live `slotknee-models` dataset and is auto-applied by the submit
   kernel; it was fitted at +0.0010 pooled (below the 0.0023 gate) with extreme per-label weights
   (Lateral Meniscus [1.0, 0.0]). No kernel pins its Docker image, the decoder wheels are cp312-only
   with `check=False` installs, and no kernel-version → code-version → git-SHA mapping exists.
3. **The gate cannot resolve what it has been asked to resolve** (C1-validation-1, -3, -4,
   C7-experiment-design-1, -6). The 0.0012 floor / 0.0023 gate rests on one seed pair whose own
   bootstrap CI is [−0.0003, +0.0026]; a null arm passes the gate 11–15 % of the time; the expected
   best of 40 null arms is +0.0041, and every adopt since 09-06 (central +0.0025, zoomc +0.003,
   zoomm +0.0016/+0.0028, two-band +0.0008) sits inside that band. Scanner-grouped folds cover
   624/4,407 studies (the other 3,783 are report-hash singletons = a stratified random split).
4. **The label contract points ~1,750 cells the wrong way and the metric cannot see it**
   (C3-labels-1, -2). v4 = mean(A, hard-B): where A ≥ 0.8 and B = 0 the target lands at ~0.42 and is
   trained as a negative at w ≈ 0.65 — Medial Meniscus 620 cells (reverse 0), Lateral Meniscus 476 (1),
   Contusion 407 (24); a third reader says YES on 84–90 % of them and the four gold rows with the
   pattern are 4/4 gold-positive. A perfect expert-protocol ranker scores 0.800 against v4 on the 58
   gold rows, below the models' 0.88, so pooled OOF vs v4 rewards LLM-protocol behaviour and closes
   label-side arms by construction.
5. **Final selection and the release path are not pre-registered** (C1-validation-6, C6-rules-compliance-1,
   -2, C7-experiment-design-5). POLICY 1 (pooled OOF only) cannot price TTA, fold-averaging or full-fit
   — the levers behind the +0.021 OOF→LB gap — and cannot select the efficiency student, whose prize
   eligibility requires it to be one of the two Final Submissions; the 09-16 plan proposes deciding
   full-fit members by one public-LB read each (sd 0.0065 at n≈390). Verbatim report text (24 fixtures)
   and `train_gold.csv` sit on the mandatory public-release path (§2.4.b, MIRA RUA §4).

---

## 2. Findings by dimension

Format per finding: **id (severity)** — title. Verdict + reason · Evidence · Failure scenario · Fix · Cost.

### 2.1 Validation methodology (C1)

**C1-validation-1 (high)** — "Grouped folds by scanner fingerprint" holds for 14 % of the training set.
- Verdict: **CONFIRMED.** Re-read `fold_recompute.csv`: 624 rows in 146 `s:` groups, 3,783 in `r:`
  groups (3,633 unique among the 3,758 off-disk rows); `fold_local == fold_oof` for all 4,407 rows.
  `kaggle/train_kernel_final/slotknee_train_final.py:134-153` symlinks exactly the 649 laptop studies and
  says so ("a whole-tree link would fingerprint all 4,407 and produce a third, different split").
  Runbook CURRENT STATE and `src/folds.py` present scanner hold-out as the OOF guarantee.
- Evidence: grouped 649 vs ungrouped 3,758: cov16 0.8783 vs 0.8816, cen16 0.8782 vs 0.8845, ensemble
  0.8808 vs 0.8857 (`lb_noise.out`); 8 groups / 77 rows straddle folds because gold kept-folds override
  group inheritance; the 0.053 inflation figure traces to a pre-pivot model.
- Failure: arms that learn scanner shortcuts receive OOF credit that does not transfer; the within-scanner
  bias is bounded at ~0.005 by the subset comparison, not 0.053; the docs promise a protection that does
  not exist.
- Fix: correct runbook/spec/`folds.py` wording now; build one whole-tree fingerprint split (CPU kernel)
  only if another gating campaign is planned, and re-baseline the two 8-ep controls.
- Cost: 15 min docs; ~3 h T4 + one CPU kernel only if re-gating.

**C1-validation-2 (high)** — Uncommitted change set selects checkpoints on 10–13 gold studies and makes
`compare_arms` score gold rows only.
- Verdict: **CONFIRMED.** `git diff main`: worktree `scripts/train_slotknee.py:1045-1047`
  `val_auc = val_metrics["val_auc_gold"]` with derived fallback, `is_gold=` written into every OOF npz
  (line 1070); main line 1050 `val_auc = val_metrics["val_auc_derived"]`. `scripts/compare_arms.py`
  worktree: `gold_only: bool = True` at lines 144, 195, 276 and in `noise_from_seeds`; `main` has zero
  occurrences. `grep val_auc_derived tests/` → nothing.
- Evidence: gold-selected epochs replayed on the fold logs (`best_epoch.out`): cen16 13/10/12/15/14
  instead of 16 (−0.0013/−0.0056/−0.0021/−0.0003/−0.0007 derived); zoomm fold 1 → epoch 4 (−0.0186);
  ssl_ep30 fold 0 → epoch 2 (−0.0909). Bootstrap sd of macro AUC on 58 gold rows 0.0154 vs a gate of
  0.0023.
- Failure: the next `package && push-code` ships it; every later fold checkpoint is an earlier epoch chosen
  on ~11 studies and every `compare_arms` verdict is computed on ~22 studies with ~10× the gate's noise,
  silently.
- Fix: keep selection on `val_auc_derived` (log gold, never select on it); default `compare_arms` to all
  studies with an explicit `--gold-only`; add a test asserting the selection key; do not push the working
  tree until reverted.
- Cost: 30 min; no retraining.

**C1-validation-3 (high)** — Noise floor 0.0012 / gate 0.0023 rest on one seed pair whose CI includes zero.
- Verdict: **CONFIRMED.** `oof_analysis.out`: s42 0.8650 vs s1337 0.8662 (diff 0.0012), paired bootstrap
  CI [−0.0003, +0.0026], sd 0.0008. `gate_stats.out`: one-observation 95 % CI for the seed scale
  [0.0005, 0.038]. `compare_arms.py:333-355` hard-codes the n=1 estimator; the 16-ep gate borrows the 8-ep
  number (runbook: "no 16-epoch seed pair exists").
- Evidence: per-label seed noise mean |d| 0.0021, max 0.0061 (LatMen) while verdict tables cite ±0.004
  per-label moves; central_e16 +0.0025 (per-fold +0.0012/+0.0036, CI [−0.0001, +0.0052], P(d≤0)=0.043);
  expected false adopts among ~40 gated arms 0.4–5.0 depending on the true sd.
- Failure: GPU sessions spent on arms whose +0.003 is inside the floor; per-label narratives steer the
  queue on noise. The core recipe itself is safe.
- Fix: two more seeds of `recipe_e16` on folds 0-1; paired bootstrap CI and per-fold sign agreement in
  `compare_arms`; per-label deltas < 0.006 treated as noise; remove stale 0.0005 quotes.
- Cost: ~3 h T4 + ~1 h scripting.

**C1-validation-4 (medium)** — Two-band ensemble measured with a non-deployment combiner and adopted under
a different rule.
- Verdict: **CONFIRMED.** `lb_noise.out` line 1: global-rank 0.8851 | within-fold-rank pooled 0.8843 |
  prob-avg 0.8855 | logit-avg 0.8853; the submit kernel rank-averages globally. Runbook 09-16 adopts at
  +0.0008 "POLICY 1 selects by pooled OOF" while every other change is gated at 0.0023.
- Evidence: deployment-matched delta +0.0015, CI [+0.0007, +0.0023], P(d≤0)=0.000 — so the decision
  was right; the 58-row tie-breaker (0.8806 vs 0.8805) has delta sd 0.007 and per-label sd 0.05–0.06.
- Failure: the next combiner decisions (student, zoomc, full-fit members) are read against a mis-specified
  estimate and noise-level tie-breakers.
- Fix: global-rank and probability-average combine modes in `compare_arms`; one written ensembling rule
  (argmax OOF with a paired CI excluding 0); never cite 58-row per-label AUCs without CIs.
- Cost: 30 min.

**C1-validation-5 (medium)** — Self-distillation OOF inflation is unquantified; the runbook's own rule is
ignored.
- Verdict: **CONFIRMED.** Runbook line 145: "a distilled run's OOF is INFLATED … NOT a gate metric";
  every ablate arm and control carries `--distill-weight 0.5`; `teacher_g10_v3.csv` is the cov16 OOF
  (Spearman 1.000, `oof_analysis.out` line 36), so a v3-trained student/5-fold is second-round
  self-distillation.
- Evidence: leak path teacher T_j (trained on fold k) → OOF on fold j → student S_k targets. The +0.0185
  distillation gain, the 0.8843 pooled OOF and the +0.021 OOF→LB offset include an unknown inflation.
- Failure: absolute OOF and the OOF→LB offset are biased upward by an unknown amount; a v3-teacher
  5-fold would look better on OOF for reasons the LB will not reward.
- Fix: one 2-fold `recipe_e16 --distill-weight 0` arm to bound the inflation; gate teacher choices on
  LB/expert labels only.
- Cost: ~3 h T4.

**C1-validation-6 (high)** — POLICY 1 cannot price fold-averaging, TTA or full-fit; the proposed workaround
is LB selection below noise.
- Verdict: **CONFIRMED.** `docs/gold_campaign.md:8` POLICY 1 "chosen by pooled OOF … never public LB";
  runbook 1058-1064 proposes full-fit members "measured by exactly one submission each";
  `lb_noise.out`: macro-AUC sd 0.0065 on 390-study subsets, paired delta sd 0.0026.
- Evidence: OOF 0.8843 → LB 0.905; the test-time prediction is a 5-fold average per band plus 3-shift TTA
  (+0.0044) whereas the OOF is one 80 %-data model without TTA; the model scores the same against expert
  and LLM labels on the 58 gold rows, so cleaner test labels do not explain the gap.
- Failure: the two final picks are chosen by a coin flip on the public LB or blocked by a policy that cannot
  see the lever; the private split will not reproduce a sub-noise public ordering.
- Fix: pre-register (i) recipe decisions by OOF, (ii) test-time composition by prior with LB as a sanity
  check only at ≥ 0.006, (iii) the OOF-selected 10-fold entry as one final pick, (iv) 10 folds + full-fit
  members as the other, submitted once.
- Cost: docs + 1–2 submissions.

**C1-validation-7 (low)** — Hygiene: partial-overlap confirmations, gold-driven straddling, stale floor
text, no CIs in the gate tool.
- Verdict: **CONFIRMED.** `folds_analysis.out`: 8 groups / 77 rows straddle; 649 distinct PatientIDs among
  649 studies (patient grouping unverifiable for 85 %); `compare_arms` prints no CI.
- Failure: confirmation numbers read as independent when folds 0-1 are 40 % of the 5-fold "confirmation".
- Fix: report unselected-fold deltas separately; annotate stale 0.0005 quotes; CI column.
- Cost: < 1 h.

### 2.2 Train/serve skew (C2)

**C2-serve-skew-1 (medium)** — Group-position embedding rows 6–9 never trained; anchor positions
re-indexed between train (6 sampled → 0..5) and serve (10 → 0..9).
- Verdict: **CONFIRMED.** `scripts/train_slotknee.py:325-328` re-indexes kept anchors; `src/slotknee.py:994`
  adds `group_embed[:G]` by position. `group_embed_check.json`: t35 vs g10t1 rows 6–9 max |diff|
  6.5e-07 (rows 0–5: 0.054); student rows 6–9 = 0.98678 × t35 (weight decay only).
- Failure: 40 % of anchor tokens carry random position codes at every inference; expected AUC cost ~0
  (multi-bag read-out measured ≤ full read-out) but it confounds every anchor-position experiment.
- Fix: rescore OOF with rows 6–9 zeroed / rows 0–5 resampled; if anything moves beyond the gate, gather
  `group_embed[keep]` and retrain.
- Cost: ~1 h rescore; 8 h per set if a retrain is needed.

**C2-serve-skew-2 (medium)** — `infer_slotknee.py --cache` is dead code; the all-0.5 "cache" prediction was
a crash artefact.
- Verdict: **CONFIRMED.** `scripts/infer_slotknee.py:267-271` yields `np.asarray(x)`; `flush()` line 334
  takes `len(buf_x[0])` as the shift count. `cache_path_crash.log` ends in
  `ValueError: expected x [B, 6, G, 1, 224, 224], got (4, 10, 1, 224, 224)`; `pred_t35_cache.csv` is all
  0.5; `pred_t35_cache_fixed.csv` matches the DICOM path to 1.6e-07 (re-verified).
- Failure: the only cache-vs-serve cross-check cannot run and its seeded output looks like a prediction.
- Fix: `yield [np.asarray(x)], …`; a 2-study test asserting no 0.5 rows.
- Cost: 10 min.

**C2-serve-skew-3 (medium)** — Per-study failures are silent: 0.5 rows, exit 0, no rate guard.
- Verdict: **CONFIRMED.** `infer_slotknee.py:160-164, 409-412, 435-438`; `slotknee_submit.py:245-250`
  treats any exit-0 group as success; `src/slots.py:469-475` accepts only `.dcm` names.
- Failure: a private-only naming/layout variant scores 0.5 on that portion with no alarm.
- Fix: `sys.exit(3)` when failures > max(2, 2 %); print the failure fraction; accept extension-less files.
- Cost: 15 min.

**C2-serve-skew-4 (low)** — dicomsdl parity on JPEG-family syntaxes untested; no fallback when it succeeds.
- Verdict: **CONFIRMED.** `compressed_parity.json`: RLE bit-exact; J2K/JPEG-LS/JPEG-Lossless could not be
  encoded locally. `src/slots.py:1350-1357` returns dicomsdl output whenever it does not raise.
- Failure: JPEG-family hidden-set DICOMs scored on pixels the model never saw, silently.
- Fix: route only verified syntaxes through dicomsdl; pydicom stack for the rest.
- Cost: 4 lines.

**C2-serve-skew-5 (low)** — Submit-kernel grouping key weaker than infer's `_check_layouts`.
- Verdict: **CONFIRMED.** `slotknee_submit.py:147-149` omits `zoom_center`/`zoom_spec`/`slot_names`.
- Failure: a zoomc set with equal zoom_mm lands in one group; infer exits; 10 checkpoints vanish.
- Fix: add the three keys. Cost: 1 line.

**C2-serve-skew-6 (low)** — Efficiency kernel carries ~70–80 s of avoidable overhead.
- Verdict: **CONFIRMED.** eff-v3 log: infer launched at 23.0 s, first print at 68.2 s; albumentations
  offline version check; pip 9→17 s; double checkpoint load.
- Failure: ~0.001 AUC-equivalent at the efficiency exchange rate. Fix: `NO_ALBUMENTATIONS_UPDATE=1`,
  lazy imports, skip pip when importable. Cost: 20 min.

**C2-serve-skew-7 (low)** — CPU fallback tie-break picks the central set and assumes 1,300 studies.
- Verdict: **CONFIRMED.** `slotknee_submit.py:198` key `(k[2]*k[3], len(groups[k]))` ties at (60, 5);
  `fold_0_best.pt` sorts first. `CPU_TEST_STUDIES=1300` is an assumption.
- Fix: explicit tie-break; derive the count from `test.csv`. Cost: 5 min.

**C2-serve-skew-8 (low)** — Trim/crop provenance defaults silently; t35 TTA gain unmeasured.
- Verdict: **CONFIRMED.** `infer_slotknee.py:79-80` defaults to 0.15/140; `pred_t35_dicom_tta3.csv`
  moves probabilities by up to 0.027; TTA priced only on the 0.15 layout (+0.0044).
- Fix: trainer refuses a cache index without `trim_frac`/`crop_mm`; measure t35 TTA via `rescore_oof.py`.
- Cost: 3 lines + one rescore.

### 2.3 Data and labels (C3)

**C3-labels-1 (high)** — v4 blend lets reader B's hard 0 veto two positive readers on ~1,750 cells.
- Verdict: **CONFIRMED.** `label_stats.out`: A-pos/B-neg vs reverse — Medial Meniscus 620/0, Lateral
  Meniscus 476/1, Contusion 407/24. `ceiling_and_reverse.out`: on those cells A ≥ 0.8 in 89 %/89 %/94 %,
  v4 target mean 0.42/0.42/0.44 (92–96 % < 0.5), trained at w ≈ 0.63–0.72. `history_bias.out`: pilkwang
  YES 88.3 % (MedMen, n=566 no-cue), 90.1 % (LatMen, n=413), 83.7 % (Contusion, n=325). Four gold rows
  with the pattern are 4/4 gold-positive. The AUC cost is a direction plus a cell count — unmeasured.
- Failure: the hidden set is expert-labelled; 12–15 % of the weakest label's cells are pushed the wrong
  way while OOF-vs-v4 rewards it.
- Fix: build `v4_majority.csv` (2-of-3 majority among A > 0.5, B > 0.5, pilkwang YES → y := A); one
  2-fold 16-ep arm gated on gold-58 + pilkwang + A/B-agree cells, not on OOF-vs-v4.
- Cost: ~1 h scripting + ~2.5–3 h T4.

**C3-labels-2 (high)** — Pooled OOF vs v4 cannot reward moving toward the expert protocol; two live
documents still carry the retracted polarity.
- Verdict: **CONFIRMED.** `docs/research_improvements_20260824.md:1385-1387` states the expert-annotation
  target and the polarity retraction. `ceiling_and_reverse.out`: a perfect gold-protocol ranker scores
  0.8002 macro vs v4 on the 58 rows (Synovitis 0.682, Fracture 0.755) — below the models' 0.88 on the
  same rows; 99/696 cells (14.2 %) contradicted by all three reads. `gold_vs_readers.out`: v4 0.8927,
  A 0.8873 vs gold. Memory note and `src/llm_labels.py:6-8` still say gold is the unreliable side.
- Failure: any label-side change that improves expert-protocol AUC reads NULL/REGRESS and is closed
  (synovitis fill, refined labels, v5/v6 all closed this way). Image-side arms are unaffected — the LB has
  tracked OOF 4/4 times for those.
- Fix: one paragraph in CURRENT STATE and the memory note; a label-side gate script scoring gold-58,
  pilkwang YES/NO and A/B-agree cells; one public-LB probe per label-set candidate as a POLICY 1 carve-out.
- Cost: docs 1 h; gate script 2 h; per probe ~1 h T4 + 1 submission.

**C3-labels-3 (medium)** — Gold 8× is an ungated hyper-parameter supplying a third of Fracture/Synovitis
positive mass.
- Verdict: **CONFIRMED.** `ceiling_and_reverse.out` gold-mass table: Fracture 38.8 % of positive mass,
  Synovitis 33.5 %; report-contradicting gold cells = 17.4 % of Synovitis positive mass; gold rows carry
  5.02 v4 positives/study vs 3.24; `gold32` never produced a verdict.
- Failure: the two rarest heads are shaped by a handful of memorised, enriched cases; direction unknown.
- Fix: 2-fold gold-weight 2 vs 8 arm gated per C3-labels-2, or weight 8 only where gold agrees with at
  least one LLM read.
- Cost: 1 h contract change or 2 × 2.5 h T4.

**C3-labels-4 (medium)** — Silent-cell semantics: `src/llm_labels.py` documents a weight path no deployed
run uses; recipe tables omit `--weights-from`.
- Verdict: **PARTIAL.** The docstring (`src/llm_labels.py:20-29, 39-42`) is wrong as stated (v4 silence at
  0.25 → weight 0.5, no `--weights-from` in the recommended order) — confirmed. But the runbook does carry
  `--weights-from` in the header recipe (line 13) and the Local command (line 71); it is the CURRENT STATE
  adopted-recipe line and THE CURRENT RECIPE table that omit it. All 10 kernel/queue scripts pass it.
- Failure: a reproduction from the docstring or spec silently trains A-silent cells as half-weight
  negatives (−0.025 macro when scored that way); its OOF is not comparable.
- Fix: trainer banner + npz metadata for the weight source; correct the docstring; add the flag to the two
  recipe tables and the spec.
- Cost: 30 min.

**C3-labels-5 (low)** — Third-party label files carry no provenance, version, hash or licence record.
- Verdict: **CONFIRMED.** No LICENSE/README/manifest under `data_subset/labels_external`; 0 files tracked;
  `kaggle/licenses` holds only the DINOv3 licence; reader B exists only as 2·v4 − v2. Both datasets are
  CC0 per the Kaggle API (licence-clean).
- Fix: `PROVENANCE.md` with slug, version, licence, date, sha256 and derivations. Cost: 30 min.

**C3-labels-6 (low)** — Clinical-history leakage: negligible post-operative leakage; no suspicion-cue effect
on the definition-split labels; mild OA effect only.
- Verdict: **CONFIRMED** (as a measured null). `report_audit.out`: 1 ACL reconstruction, 17 prior-surgery
  mentions; `history_bias.out`: no cue effect on meniscus/Contusion/ACL; Lateral OA 0.249 vs 0.088 on
  n = 177–245 cue reports (could be report style).
- Fix: strip indication sections on any re-extraction. Cost: 30 min; nothing to change now.

### 2.4 Code correctness (C4)

**C4-code-1 (critical)** — Best-epoch selection moved to the ~12-study gold AUC (known-defect #3
re-introduced) — worktree only.
- Verdict: **CONFIRMED** (same defect as C1-validation-2 / C8-docs-and-process-1; re-checked this pass).
  Gold-best differs from derived-best in 15 of 17 logged folds; 12/10/12/11/13 gold studies per fold.
- Fix: revert to `val_auc_derived`; unit test; no push until fixed. Cost: 5 min + 1 test.

**C4-code-2 (critical)** — `compare_arms.py` defaults to gold-only pooling and silently mixes gold-only
arms with all-study baselines — worktree only.
- Verdict: **CONFIRMED.** `gold_only: bool = True` defaults at lines 144/195/276 and in `noise_from_seeds`;
  `main` has none. Synthetic check: baseline without `is_gold` vs identical-logit arm with `is_gold` →
  n 12 vs 200, delta +0.0172, ADOPT, no note. Today's OOFs lack the key so the tool still prints
  0.8836 / 0.8812.
- Fix: default `gold_only=False`, explicit `--gold-only`; raise when baseline and arm differ in `is_gold`
  availability or n by > 10 %; test the mixed case. Cost: 10–15 min.

**C4-code-3 (high)** — Scanner-grouped folds cover 14 %; `build_groups` falls back silently; gold
keep-folds split scanner groups.
- Verdict: **CONFIRMED** (= C1-validation-1). `src/folds.py:187-189` gives any study without a local
  directory an `r:<hash>` group with no counter; the kernel prints only a WARN when `fold_image_uids.txt`
  is missing; 8 scanner groups (77 studies) straddle; 22 of 58 gold rows have a same-scanner study in a
  training fold.
- Fix: print/refuse partial coverage without `--allow-partial-groups`; document the 649-uid trick; a
  scanner-first re-split only with a new baseline. Cost: 30 min; ~8 GPU-h if re-split.

**C4-code-4 (high)** — `infer_slotknee.py --cache` crashes.
- Verdict: **CONFIRMED** (= C2-serve-skew-2). Fix: yield a one-element shift list; smoke test. Cost: 2 lines.

**C4-code-5 (medium)** — `group_embed` indexed by bag position; rows 6–9 never trained.
- Verdict: **CONFIRMED** (= C2-serve-skew-1). Row norms 6–9 identical in two independent runs.
- Fix: pass `group_ids` to `forward`; gate with one 2-fold arm. Cost: ~20 lines + ~3 GPU-h.

**C4-code-6 (low)** — `group_map` hard-codes 12 labels while `n_labels` is free.
- Verdict: **CONFIRMED** (saved check: `SlotKneeS(n_labels=3)` builds, forward fails on a 12-vs-3
  mismatch; not re-run this pass). Fix: derive from `n_labels` or assert. Cost: 5 min.

**C4-code-7 (low)** — `--limit-minutes` clock is global across folds; a truncated fold pools silently.
- Verdict: **CONFIRMED.** `t_start` set once before the fold loop; OOF npz carries no epoch count. Not hit
  on the deployed sets (16/16 epochs with the completed marker on every fold).
- Fix: per-fold clock or `n_epochs_run` in the npz. Cost: 10 min.

**C4-code-8 (low)** — Each checkpoint is `torch.load`-ed 2–3 times in infer.
- Verdict: **CONFIRMED.** Lines 75/306/316. Fix: load once. Cost: 5 min.

### 2.5 Kaggle operations (C5)

**C5-kaggle-ops-1 (critical)** — Live `slotknee-models` was mutated after the 0.905 submission: the next
accuracy run silently applies unscored, sub-gate stacking weights.
- Verdict: **CONFIRMED.** Re-checked: `stack_weights.json` mtime 2026-09-16 20:04:38; the eleven `.pt`
  files 09:33:19–20; content `prefixes ['', 'g10t1']`, Lateral Meniscus [1.0, 0.0], ACL [0.95, 0.05],
  Medial Meniscus [0.05, 0.95], note "+0.0010 pooled vs equal weights". `slotknee_submit.py:159-172`
  globs and applies any `stack_weights*.json`; `push_models()` stages only `fold_*_best.pt` and
  `fold_*_log.json`, never the weights file. Today's read-only `dataset_list_files` (saved in
  `C5-kaggle-ops.md`) lists it in the live dataset; the pulled v19 output reproduces exactly as the
  equal-weight rank average, so v19 ran without it.
- Failure: any re-run of `slotknee-submit` (final-selection re-run, the Oct 21-22 buffer, the next
  push-models version) produces an ensemble never scored on the LB, failing the campaign's own gate, with
  one log line as the only signal. Expected AUC effect is small (+0.0010 OOF) but the entry is no longer
  reproducible from "latest".
- Fix: push a models version without the file (or rename `.DISABLED`); kernel applies stacking only from
  `stack_weights_ADOPTED.json`; runbook line "kernel vN ran with models-dataset vM (file list)".
- Cost: 10 min + one dataset push.

**C5-kaggle-ops-2 (high)** — API GPU pushes now start real 7–9 h sessions, but the header, docstrings and
memory still say "API = P100 fast-fail"; `push_kernel` has no guard.
- Verdict: **CONFIRMED.** Runbook lines 30-33 ("Kaggle's API always assigns a P100 … fails fast") and
  125-127 ("a GPU-enabled API push doubles as a free quota check") re-read; the CURRENT STATE table
  records sessions 5 (v32) and 6 (v33) as DONE from their pushes. `kaggle_ops.py:204-215` calls
  `kernels_push` unconditionally with no `--cpu`/dry-run and no running-session check.
- Failure: a "quota check" push burns 7–9 of the 30 weekly GPU hours or blocks a UI launch; two accidents
  slip the final retrain by a week.
- Fix: `push_kernel(which, gpu=False)` writing temp metadata with `enable_gpu:false`; explicit `--gpu-run`;
  print `kernels_status` first; rewrite the runbook lines and both memory notes.
- Cost: 30 min.

**C5-kaggle-ops-3 (high)** — No kernel pins its Docker environment; decoder wheels are cp312-only and their
install is non-fatal.
- Verdict: **CONFIRMED.** `grep docker_image_pinning_type kaggle/*/kernel-metadata.json` → none;
  `kaggle/wheels/`: dicomsdl, pylibjpeg-libjpeg, pylibjpeg-openjpeg all `cp312`; `slotknee_submit.py:104,
  109` `check=False`; no version banner. (Verified not a risk: `torch.load(weights_only=True)` succeeds on
  all three checkpoint families.)
- Failure: a base-image bump before the deadline makes the wheels fail; pylibjpeg absent means JPEG-family
  DICOMs are zero-filled and scored from blank slots; the selected version re-run on "latest" differs from
  the scored one.
- Fix: `"docker_image_pinning_type": "original"` on both submit kernels (and train kernels), re-push once;
  print versions; fatal decoder self-check when a compressed syntax is present; add cp313 wheels if available.
- Cost: 20 min + one push per submit kernel.

**C5-kaggle-ops-4 (medium)** — Submission provenance not recorded (kernel version → code-dataset version →
git SHA).
- Verdict: **CONFIRMED.** `push_code` uses a timestamp as `version_notes`; `package()` copies the working
  tree (17 files differ today); the only durable record is scriptVersionId 350248733 / 350318598.
- Fix: `MANIFEST.json` (SHA, dirty flag, per-file sha256); SHA as version notes; runbook columns.
- Cost: 30 min.

**C5-kaggle-ops-5 (medium)** — "Seed a scoreable file first" does not cover wrapper-level failures or a
silently constant merge; the local validator rejects valid rank outputs.
- Verdict: **CONFIRMED.** Unguarded `torch.load` per checkpoint (line 143); unasserted merge (251-266);
  `submit_v19_watch.log` "values in (0,1): False … NOT submitted" on a legitimate max rank of 1.0.
- Fix: try/except per checkpoint; post-merge asserts; print n_test and ETA; watcher accepts [0, 1].
- Cost: 30 min.

**C5-kaggle-ops-6 (medium)** — `push-models` replaces the whole dataset with whatever dirs are passed; both
submit kernels keep a stale `kernel_sources` with a silent fallback.
- Verdict: **CONFIRMED.** `push_models` rmtrees the stage and uploads only the given entries; line 80 of
  the submit kernel falls back to the 8-epoch `slotknee-train` output.
- Fix: `SETS.json` + `--expect`; drop `kernel_sources`; make the fallback an error. Cost: 20 min.

**C5-kaggle-ops-7 (low)** — No GPU-quota ledger; ~21 of 30 h used this week, five resets left.
- Verdict: **CONFIRMED** (runbook narrative only). Fix: `docs/quota_ledger.md`; `status --all` before any
  GPU push. Cost: 15 min.

**C5-kaggle-ops-8 (low)** — Kernel inventory drift (28 kernels on the account, 9 missing from the table,
one orphan, stale docstrings).
- Verdict: **CONFIRMED.** Fix: regenerate the table from `kernels_list`; fix docstrings. Cost: 15 min.

**C5-kaggle-ops-9 (low)** — Single account, single operator, single copy; no final-week freeze checklist.
- Verdict: **CONFIRMED.** Scored checkpoints exist locally only in gitignored `kaggle/slotknee_models/`;
  dataset files cannot be downloaded back.
- Fix: sha256 + off-machine copy of the 11 checkpoints; freeze checklist (section 5). Cost: 30 min.

### 2.6 Rules and licences (C6)

**C6-rules-compliance-1 (high)** — Efficiency-prize eligibility hinges on Final-Submission selection, and
POLICY 1 cannot pick the efficiency student.
- Verdict: **PARTIAL.** The rule text is confirmed (excerpt: "Must be among the submissions selected by a
  team for the Leaderboard Prize…"; §2.2.b two Final Submissions) and POLICY 1 (`gold_campaign.md:8`)
  says OOF only, while runbook 487 says the student "is judged only on the public LB". But the project
  already has a two-slot rule — runbook Decision rules ("Keep two selected submissions on Kaggle:
  best-AUC ensemble and the fast student") and `gold_campaign.md:19`. What is missing is reconciling
  the two written rules and a UI verification step, not the rule itself.
- Failure: at the deadline the slots are filled per POLICY 1 or by Kaggle auto-select after later probing,
  and the student is not among them.
- Fix: amend POLICY 1 with the explicit two-slot rule; "confirm the two Selected check-marks" after every
  probe and in the Oct 21-22 buffer. Cost: 15 min + one UI check per probe.

**C6-rules-compliance-2 (medium)** — Verbatim competition report text and label subsets sit on the
mandatory public-release path (§2.4.b, MIRA RUA §4).
- Verdict: **CONFIRMED.** 24 tracked quoted strings byte-identical to `train.csv` reports
  (`tests/test_labels.py` 20, `src/labels.py` 4; saved script output); `kaggle_ops.py:82-88` copies
  `labels_external/` and `train_gold.csv` (58 competition label rows) into `slotknee-code` (re-read).
  Nothing is public today.
- Failure: the winners' package publishes report sentences and label rows; §3.9.b one-week remediation
  inside the Nov 5 crunch.
- Fix: paraphrase the fixtures keeping cue/negation structure; replace the four comment quotes; derive
  the 58 rows from `train.csv` at runtime instead of shipping `train_gold.csv`. Cost: 1–2 h.

**C6-rules-compliance-3 (medium)** — CC0 declarations are false for the public release; unused DINOv3
weights carry Meta's obligations; Apache notices missing.
- Verdict: **CONFIRMED.** `kaggle_ops.py:141, 191` declare CC0-1.0 for both datasets; line 114 packages
  `vit_small_patch16_dinov3.lvd1689m` (no arm ever measured); no Apache LICENSE/NOTICE for the DINOv2
  derivatives; `pylibjpeg-libjpeg` is GPL-3.0 (OSI-approved, so §3.6.c is satisfied).
- Fix: drop the DINOv3 file from `wanted`; add Apache notice; set both licences to "Other (specified in
  description)" with a per-component table; re-push before going public. Cost: ~30 min + one version each.

**C6-rules-compliance-4 (medium)** — The §2.8.a winners' package is neither reproducible nor tracked (no
image id, caches private, no writeup).
- Verdict: **CONFIRMED.** No `docker_image_pinning_type`/`dockerImageVersionId` anywhere; train-final
  depends on two private cache kernels (14+ GB derived images that cannot be published); no
  writeup/model-card file.
- Fix: pull the image versions now; `docs/winners_package.md` with environment, reproduction order, licence
  table, forum/video/Kaggle Models steps. Cost: ~2 h now.

**C6-rules-compliance-5 (low)** — The "public GitHub repo" premise is false today; flipping it public
before Oct 22 would trigger §3.6.b and §2.4.b.
- Verdict: **CONFIRMED.** Remote returns 404 unauthenticated. Fix: keep private until after Oct 22. Cost: 0.

**C6-rules-compliance-6 (low)** — No tooling can verify kernel/dataset privacy.
- Verdict: **CONFIRMED.** `kernels_list` reports `isPrivate=False` for all 28 private kernels; 22 metadata
  files use the string "true". Fix: `check-visibility` via unauthenticated GET; normalise the bool.
  Cost: 20 min.

**C6-rules-compliance-7 (low)** — OrthoFoundation-L and KMAR-50K remain undecided; one forum question
settles it.
- Verdict: **CONFIRMED.** Neither asset is in any entry; SSL is closed so KMAR-50K is moot.
- Fix: post the host question before any OrthoFoundation session; mark KMAR-50K moot. Cost: 10 min.

### 2.7 Experimental design (C7)

**C7-experiment-design-1 (high)** — ADOPT gate is a single-draw noise estimate with an 11–15 % per-arm
false-ADOPT rate; central_e16's adopt is z ≈ 1.3.
- Verdict: **CONFIRMED.** `compare_arms.py:199-206` (ADOPT if delta > 2·noise) and `noise_from_seeds`
  (one pair, max of naive/paired) re-read; `supplement.log`: false-ADOPT per null arm 0.137 (one control),
  0.106 (both controls), 0.147 (16-ep arms on the borrowed gate); power at a true +0.0022 effect 41 %;
  CV of |D| as a sigma estimate 0.755. The bootstrap estimators in `analysis_stdout.log` give central
  P(d≤0)=0.027–0.043; adding seed variance gives z ≈ 1.3.
- Failure: ~1.5 expected false adopts among ~12 gated arms, each costing a 7-h retrain.
- Fix: `central_e16_s1337` on t35 for the 16-ep pair; verdict = paired bootstrap CI excluding 0 AND sign
  agreement on both folds AND delta > t·sqrt(bootSD² + σ_delta²). Cost: 176 min T4 + ~2 h scripting.

**C7-experiment-design-2 (high)** — ~73 % of sessions 4–6 GPU time went to arms that could not change a
decision, while e24 / zoomc@16 / sag_spec / the seed pair stay unrun.
- Verdict: **PARTIAL.** The displacement is confirmed: `ARMS` (ablate kernel lines 92-150) puts
  `central_flip_e16` (~185 min) and `zoomm` (~105 min) ahead of `recipe_e24` (~285 min) in a 500-min
  session, so e24 could not start; `recipe_e24`, `sag_spec` and `zoomc` sit at positions 8–10; the runbook
  called e24 "the most valuable single measurement now" on 09-06. But "could not change a decision" is
  too strong for two of the arms: `central_flip_e16` was the runbook's declared 16-ep test of a
  MARGINAL result (it settled it as NULL), and `ssl_ep10` ruled out "too little pretraining". The
  allocation critique stands; the hour count is an upper bound.
- Failure: five quota weeks spent on hygiene arms with the two cheapest positive-EV levers unmeasured.
- Fix: re-order `ARMS`: seed pair → e24 on t35 → zoomc@16 on t35 (needs a t35 zoomc cache) → sag_spec;
  stop rules (drop a family after a first REGRESS < −0.02; never queue an arm that cannot finish).
- Cost: edit + one CPU cache build.

**C7-experiment-design-3 (high)** — Premature closures: LoRA, SSL, resolution and T=3 were "closed" by
designs that could not have adopted them.
- Verdict: **PARTIAL.** LoRA and SSL confirmed as design-limited: runbook 895-897 concedes LoRA at lr 1e-4
  / 8 ep "may be under-trained … parked"; 1012-1016 closes SSL with 8 of 12 blocks frozen after MAE
  changed all 12. Resolution and g10t3 are weaker: p336 was measured NULL (on the pre-coverage recipe)
  and g10t3 fold-0 −0.004 was measured (one fold, 1.5–2 sd) — under-powered, not impossible. The
  "Closed (measured, do not reopen)" label overstates the evidence for all four.
- Failure: ≥ 0.005-class levers — the only class the private LB can resolve — stay unexamined.
- Fix: relabel as "unmeasured at the adopted recipe, low prior"; pick at most two by prior (LoRA lr 5e-4 @
  16 ep ~5.6 h; p336 @ 16 ep on t35 ~8 h). Cost: 0 h to relabel; 3–8 h per arm run.

**C7-experiment-design-4 (medium)** — Decision metric excludes 18.9 % of cells (w == 0), non-randomly,
and pooled-over-folds is not how the ensemble is scored.
- Verdict: **CONFIRMED.** `analysis_stdout.log` lines 87-100: Baker's 47.5 %, Fracture 42.4 %, Lateral OA
  24.3 %, Contusion 23.7 % silent; model p on silent vs scored ACL 0.163 vs 0.090, MCL 0.138 vs 0.068;
  `compare_arms.py:175-186` scores only w > 0.
- Failure: label-side and ambiguous-study arms are invisible or mis-signed to the gate.
- Fix: mean-of-folds AUC, a complete-label variant, sign agreement across the three. Cost: one afternoon.

**C7-experiment-design-5 (medium)** — Last three weeks bought effects below the private LB's resolution;
the 09-16 plan proposes public-LB reads to decide full-fit members.
- Verdict: **CONFIRMED** (= C1-validation-6). n = 910 subsampling: macro sd 0.0044, central P(delta<0)=0.09,
  two-band 0.18; n = 390 macro sd 0.0071; ~41 T4-h produced +0.0031 total.
- Fix: pre-register the full-fit rule; learning-curve arm (fold 0 at 50/75/100 %); triage arms by expected
  effect ≥ 0.005. Cost: one session + a written rule.

**C7-experiment-design-6 (medium)** — No multiple-comparison discipline: +0.0008 adopted (two-band) while
+0.0007 rejected (seed ensemble).
- Verdict: **CONFIRMED.** Expected max of 40 null arms +0.0041; Bonferroni K=40 threshold +0.0045 —
  only coverage, reg4, distil, bag6, e16 survive; central rescued only by folds 2–4 replication (+0.0020,
  CI [+0.0001, +0.0040]), a rule not written down nor applied to zoomc/zoomm.
- Fix: adopts < 0.005 only after independent replication with CI excluding 0; one ensembling rule applied
  identically to seeds, bands and layouts. Cost: documentation + one rule.

**C7-experiment-design-7 (medium)** — 2-fold/8-epoch screening does not transfer to the 16-epoch recipe
and its gate is borrowed.
- Verdict: **CONFIRMED.** flipswap +0.0027/+0.0015 at 8 ep → −0.0002 at 16 ep; per-fold deltas disagree
  3×; 4 of 6 arms in sessions 5–6 ran at 8 ep despite the kernel docstring.
- Fix: screen at 16 ep on t35; 8-ep arms only as kill-tests for ≥ 0.02 levers. Cost: re-cut `ARMS`.

**C7-experiment-design-8 (medium)** — Gold-58 numbers cited as headline evidence with 9 MCL positives.
- Verdict: **CONFIRMED.** `gate_stats.out`: MCL 9, Lateral OA 11, Baker's 12 positives; per-label sd
  0.013–0.087; paired central-minus-coverage on gold +0.0001, CI [−0.015, +0.014].
- Fix: CI next to every gold number; gold only for label-error triage. Cost: ~1 h.

**C7-experiment-design-9 (low)** — Per-label deltas below 0.01 and the post-hoc "focus delta" are narrated
as mechanism.
- Verdict: **CONFIRMED.** Seed-to-seed per-label spread with no recipe change reaches +0.0061.
- Fix: per-label reads only above 0.010 with sign agreement; drop focus delta from verdict tables.

**C7-experiment-design-10 (low)** — "val AUC still rising at 16" is a cosine-tail artefact; e24 must be
measured. Checkpoint selection itself is clean.
- Verdict: **CONFIRMED.** Last four val AUCs of the t35 folds rise +0.0008/+0.0004/+0.0001; 8-ep runs
  flatten identically. Best == last on all 16 fold logs. Fix: run `recipe_e24`. Cost: 265 min T4.

**C7-experiment-design-11 (medium)** — No written plan for the remaining five weeks; freeze dates absent
from CURRENT STATE.
- Verdict: **CONFIRMED.** CURRENT STATE carries no calendar; `gold_campaign.md` has Oct 12/17/20 that the
  runbook never mentions; e24 deferred sessions 3→4→5→6.
- Fix: the week-by-week plan in section 4. Cost: one planning pass.

### 2.8 Documentation and process (C8)

**C8-docs-and-process-1 (critical)** — Uncommitted edit re-introduces known-defect #3.
- Verdict: **CONFIRMED** (= C1-validation-2 / C4-code-1). Additionally the diff drops `val_auc_derived`
  from the per-epoch record and 17 files (333+/179−) are uncommitted, including the gate tool and
  `src/soup.py`, with no review/commit gate on the co-edited tree.
- Fix: `git checkout main -- scripts/train_slotknee.py` after salvaging wanted hunks; keep both metrics
  logged; test; review-or-revert the other 16 files. Cost: 15 min + ~30 min review.

**C8-docs-and-process-2 (critical → re-graded high)** — The 0.905 / 0.907 entries cannot be rebuilt from
the repository alone (runbook line 10 claims otherwise).
- Verdict: **CONFIRMED.** `.gitignore` excludes `data_subset/` and `kaggle/slotknee_code/`; 0 tracked
  CSVs (labels, teachers, `train_gold.csv`, `fold_image_uids.txt` all untracked); DINOv2 weights copied
  from `~/.cache/huggingface`; train-final `RECIPE` hard-codes the t35 band (the coverage band came from
  v11 whose RECIPE is not in the tree); the student kernel prefers `teacher_g10_v3.csv` while the scored
  0.907 student was v2-trained with no env knob; the two-band push command appears only once in the
  student form. Re-graded high: every artefact exists locally and on Kaggle; the gap is tracking and a
  10-line rebuild section, remediable without GPU.
- Fix: track (or fetch-script with sha256) the six CSVs and the uid list; `fetch_weights.py`; RECIPE
  dict with both bands; `SK_TEACHER` env; "Rebuild 0.905 / 0.907" section. Cost: 1–2 h.

**C8-docs-and-process-3 (high)** — README and architecture guide describe a ViT-Base 3-pass pipeline that
never produced a submission.
- Verdict: **CONFIRMED.** `README.md:15-25` re-read (3-stage Teacher-Student, ViT-Base teacher and
  5-fold, `train_slotknee_pipeline.sh` driver); `architecture_guide.md` Phase 4 still evaluates a
  DINOv2-Base teacher and Cutout/Rotation (cutrot was reverted). ViT-B measured NULL twice.
- Fix: rewrite README Execution to the kernel path; replace Phase 3/4; mark the pipeline script legacy;
  drop vit_base from the packaged weights. Cost: 1 h.

**C8-docs-and-process-4 (high)** — Runbook CURRENT STATE contradicts its own header, the dated sections and
the kernels.
- Verdict: **CONFIRMED.** Re-read: lines 10-14 "winning recipe" = 5 × 8 ep, reg4 "+0.0187" vs line 137
  "+0.0220, ACL +0.0617" (kernels agree with 0.0220); line 42 train-final "10 ep, warm-start, 2 seeds" vs
  the actual v13 (16 ep, t35, seed 42); lines 93-95 cite "gate 0.0011" in the paragraph that states the
  gate is 0.0023; lines 125-127 "Launch order: submit-eff → train-g10 → ablate session 3 → ssl" all
  already done or closed; lines 84 "≥ 2 seeds, --full-fit" vs ten 80 %-data fold models; kernel table
  lacks train-student, submit-eff, train-final v13 and the t35 builder.
- Failure: an operator following CURRENT STATE literally launches 10 h of closed questions.
- Fix: rewrite lines 10-14, 36-46, 81-88, 101-127 from the 09-16 section; supersede-banner 129-162.
  Cost: 1 h.

**C8-docs-and-process-5 (high)** — `docs/known-defects.md` audits a pipeline that is not in this
repository and says nothing about the submit path.
- Verdict: **PARTIAL.** The document is explicitly banner-labelled "HISTORICAL (pre-pivot)" at lines 3-5
  and cites `b889ea7` / `recovered/` (29 mentions) as its basis by design, so "audits a pipeline not in this
  repository" is stated intent, not drift. Confirmed parts: it is stale-as-PRESENT on D5a (autocast
  now gated by `use_amp`, `src/train.py:328-331`), D6 (`aug_hflip_p` default 0.0, `src/config.py:417`)
  and D8, and it never names `scripts/train_slotknee.py`, `infer_slotknee.py` or the submit kernel — so
  the D3 recurrence in the working tree had no register to be caught by.
- Fix: a "Status in the SlotKnee path (2026-09-16)" line per defect with file:line. Cost: 1–2 h.

**C8-docs-and-process-6 (medium)** — Spec, script defaults and the runbook Local command describe the
pre-pivot base recipe.
- Verdict: **CONFIRMED** (saved evidence: spec G=3/T=3/trim 0.15/`--epochs 6`; defaults `--epochs 6`,
  `--anchor-bag 0`, plain backbone; Local command lacks reg4/bag/distill/16 ep).
- Fix: "ADOPTED RECIPE (2026-09-16)" block in the spec; full-recipe Local command; a banner when the
  adopted flags are absent. Cost: 30 min.

**C8-docs-and-process-7 (medium)** — Coordination docs are locally excluded from git or cite absent
scripts; POLICY 5 never recorded as lifted.
- Verdict: **CONFIRMED.** `gold_campaign.md:10` still reads "GLOBAL DEPLOY FREEZE, IN FORCE" while 20+
  pushes and four submissions followed; `.git/info/exclude` hides `AI_COORDINATOR.md`, `.agents/AGENTS.md`,
  `docs/swarm_plan.md`; several docs cite `train_gold_medal.sh` / `train_ultimate_5hr.sh` (absent).
- Fix: track or delete the excluded files; `docs/INDEX.md` LIVE vs LEGACY; dated "POLICY 5 lifted" line.
  Cost: 30 min.

**C8-docs-and-process-8 (medium)** — The submit path is untested at the kernel layer; the two submit
kernels are diverging copies.
- Verdict: **CONFIRMED.** No test imports `slotknee_submit`; the eff kernel differs only in glob default and
  the missing student filter; 17 distinct cache-builder copies.
- Fix: `kaggle/submit_common.py` + 3 pytest cases with the tiny checkpoints. Cost: 2 h.

**C8-docs-and-process-9 (low)** — Dead, duplicated and laptop-only scripts; docstrings drifted from kernel
metadata.
- Verdict: **CONFIRMED.** Fix: `scripts/legacy/`; fix the g10 docstring. Cost: 20 min.

---

## 3. What held up under attack (with the checks)

- **Fold split.** `fold_local == fold_oof` for all 4,407 rows; cov16 and cen16 share fold membership; the
  58 gold rows keep their folds (12/10/12/11/13); duplicate-report groups never straddle (0/54).
- **Seed determinism across kernels.** `recipe_e16_s42` (ablate) vs the train-final 5-fold on folds 0-1:
  0.8822 vs 0.8823 pooled, per-label |diff| ≤ 0.0028.
- **No best-epoch optimism.** Best == last on every 16-ep and 8-ep fold log on disk under `main`'s
  selection metric.
- **Big adopts.** e16 +0.0160/+0.0172 vs the two 8-ep controls, CI [+0.0135, +0.0195]; coverage, reg4,
  distillation and bag6 survive Bonferroni K=40; central replicated on folds 2–4 (+0.0020, CI
  [+0.0001, +0.0040], 5/5 folds positive).
- **Pixel path.** On 8 probe studies (L/R/unresolved/missing-slot), `build_study_tensor` at inference is
  bit-identical to the cached row for trim 0.35 and 0.15 and for pydicom and dicomsdl; masks identical;
  fold-0 t35 logits differ by exactly 0.0; the corrected cache path matches to 1.6e-07.
- **Deployment integrity.** The 11 deployed checkpoints are md5-identical to `ckpt_t35`, `ckpt_g10t1_e16`,
  `ckpt_student`; v19 and eff-v3 logs show two layout groups, student excluded, TTA as designed, 3/3
  predicted, 0 failed; the v19 output is exactly the equal-weight two-band rank average of its group files
  (`np.allclose`); `torch.load(weights_only=True)` succeeds on all three checkpoint families.
- **Decode path.** dicomsdl cp312 + pylibjpeg wheels ship and the logs print `decoder: dicomsdl` on
  Python 3.12 with J2K/JPEG-Lossless decoders available; RLE re-encodes decode bit-exact under both
  decoders; 9-h budget has ~8× margin for 1,300 studies (~63 min).
- **Labels.** Extraction is not the noise source (A vs B 0.9855, pilkwang vs A 0.9825 macro AUC); A-silent
  cells cost exactly 0 (19.2 % at w=0, verified from the npz); gold-row OOF (0.88) equals the non-gold OOF,
  so gold is genuinely held out; post-operative leakage negligible (1 ACL reconstruction, 17 prior-surgery
  mentions in 4,407); all deployed kernels use identical label semantics; both label datasets are CC0.
- **Rules.** External data and pretrained models are allowed; both entries use only Apache-2.0 DINOv2-S
  reg4 and CC0 label sets; the efficiency entry ran offline on a T4 with TTA off; all 28 kernels are private
  (unauthenticated 404); no competition data is tracked in git; 6 submissions against the 5/day cap; the
  DINOv3 licence analysis is correct.
- **Code.** Checkpoints carry `base_queries` + `group_map` and strict-load into the current class;
  `build_targets` yields 0 NaN cells; masked losses guard the denominator; fp16 path sound (finfo.min
  masking, fp32 softmax/LN/BCE, unscale before clip); `teacher_g10_oof.csv` covers 4,407/4,407 so
  distillation was engaged; all five folds of both sets ran 16/16 epochs.
- **Tests.** 496 passed on `main` (246 s) and 499 on the working tree (238 s); real subprocess tests cover
  `infer_slotknee.py` (fold reference, flip TTA, layout trim, flip-swap).
- **Process.** The runbook's dated history is self-correcting (0.8657 → 0.8650 and 0.0005 → 0.0023
  annotated in place); the 09-16 numbers match the OOF files on disk; the LB pair 0.905 vs 0.907 is read
  correctly as within noise; ~90 T4-h of ablation are documented per arm.

---

## 4. Prioritised fix plan

### 4.1 No-GPU fixes (in order)

| # | id(s) | action | files | GPU |
|---|---|---|---|---|
| 1 | C1-validation-2, C4-code-1, C8-docs-and-process-1 | Revert best-epoch selection to `val_auc_derived` (keep gold AUC logged); add `tests/test_selection_metric.py` pinning the key; review-or-revert the other 16 uncommitted files; no `package && push-code` until done | `scripts/train_slotknee.py`, `tests/` | no |
| 2 | C4-code-2 | `compare_arms`: `gold_only=False` default, explicit `--gold-only`; raise on mixed `is_gold` availability / n mismatch > 10 %; test | `scripts/compare_arms.py`, `tests/test_compare_arms.py` | no |
| 3 | C5-kaggle-ops-1 | Push a `slotknee-models` version without `stack_weights.json`; kernel applies stacking only from `stack_weights_ADOPTED.json`; runbook line recording models-dataset file list per scored kernel version | `kaggle/submit_kernel/slotknee_submit.py`, `kaggle/slotknee_models/`, `docs/slotknee_runbook.md` | no (one dataset push) |
| 4 | C5-kaggle-ops-3, C6-rules-compliance-4 | `docker_image_pinning_type: original` on both submit kernels (then train kernels); print `sys.version`/torch/timm/pydicom; fatal decoder self-check on compressed syntaxes; record image ids of v13/v5/v19/eff-v3 | `kaggle/*/kernel-metadata.json`, `kaggle/submit_kernel*/`, runbook | no (one push per submit kernel) |
| 5 | C5-kaggle-ops-2 | `push_kernel(which, gpu=False)` with temp `enable_gpu:false`; `--gpu-run` flag; `kernels_status` print; rewrite runbook 30-33 / 125-127 and both memory notes | `kaggle/kaggle_ops.py`, runbook, memory | no |
| 6 | C2-serve-skew-2, C4-code-4 | Fix `_cache_stream` (one-element shift list); 2-study cache smoke test asserting no 0.5 rows | `scripts/infer_slotknee.py`, `tests/` | no |
| 7 | C2-serve-skew-3 | Failure-rate guard (`exit 3` above max(2, 2 %)); failure fraction in the kernel summary; accept extension-less files | `scripts/infer_slotknee.py`, `kaggle/submit_kernel/slotknee_submit.py`, `src/slots.py` | no |
| 8 | C1-validation-1, C4-code-3 | Correct fold-grouping wording (report-hash for all rows; scanner groups for the 649 only); `build_groups` prints coverage and the trainer refuses < 100 % without `--allow-partial-groups`; document the 649-uid symlink | `src/folds.py`, `scripts/train_slotknee.py`, `docs/slotknee_runbook.md`, `docs/slotknee_spec.md` | no |
| 9 | C1-validation-3/-4/-7, C7-experiment-design-1/-4/-8/-9 | `compare_arms`: global-rank and prob-average combine; paired study-bootstrap CI + per-fold sign agreement as the verdict; mean-of-folds and complete-label variants; CI next to gold numbers; drop focus delta from verdicts; stale 0.0005 quotes annotated | `scripts/compare_arms.py`, runbook | no |
| 10 | C1-validation-6, C6-rules-compliance-1, C7-experiment-design-5/-6/-11 | Pre-register in `gold_campaign.md`: two-slot rule (slot 1 OOF-selected accuracy entry; slot 2 efficiency student by LB + RuntimeSeconds, never displaced by a probe); full-fit rule; replication rule for adopts < 0.005; freeze calendar; UI "Selected" check after every probe | `docs/gold_campaign.md`, `docs/slotknee_runbook.md` | no |
| 11 | C3-labels-2, C3-labels-4 | CURRENT STATE paragraph: expert protocol is the target, OOF-vs-v4 gates image-side arms only; label-side gate script (gold-58 + pilkwang + A/B-agree); fix `src/llm_labels.py` docstring; trainer banner + npz metadata for the weight source; `--weights-from` in both recipe tables and the spec | runbook, `scripts/rescore_oof.py`, `src/llm_labels.py`, `scripts/train_slotknee.py`, spec, memory note | no |
| 12 | C3-labels-1 (prep) | Build `v4_majority.csv` (2-of-3 majority; y := A where the majority is positive) and diff it against v4 | `data_subset/labels_external/`, new script | no |
| 13 | C5-kaggle-ops-4, C8-docs-and-process-2 | `MANIFEST.json` (git SHA, dirty flag, sha256 per file) in `package()`; SHA as `version_notes`; track or fetch-script the six label/teacher CSVs and `fold_image_uids.txt`; `fetch_weights.py`; RECIPE dict for both bands; `SK_TEACHER` env; "Rebuild 0.905 / 0.907" runbook section | `kaggle/kaggle_ops.py`, `.gitignore`, `kaggle/train_kernel_final/`, `kaggle/train_kernel_student/`, runbook | no |
| 14 | C6-rules-compliance-2/-3, C3-labels-5 | Paraphrase the 24 report fixtures; replace the four `src/labels.py` quotes; stop shipping `train_gold.csv` (derive at runtime); drop DINOv3 and vit_base from `wanted`; Apache notice; per-component licence table on both datasets; `PROVENANCE.md` | `tests/test_labels.py`, `src/labels.py`, `kaggle/kaggle_ops.py`, `kaggle/licenses/`, `data_subset/labels_external/` | no |
| 15 | C5-kaggle-ops-5/-6, C2-serve-skew-5/-7/-8 | Submit-kernel hardening: try/except per checkpoint; post-merge asserts (no NaN, row count, ≥ 2 distinct values); n_test + ETA print; grouping key gains zoom_center/zoom_spec/slot_names; explicit CPU tie-break; `CPU_TEST_STUDIES` from `test.csv`; `push_models --expect`; drop `kernel_sources`; trainer refuses a cache index without trim/crop keys; watcher accepts [0, 1] | `kaggle/submit_kernel*/`, `kaggle/kaggle_ops.py`, `scripts/train_slotknee.py`, `scripts/infer_slotknee.py` | no |
| 16 | C8-docs-and-process-3/-4/-5/-6/-7/-9, C5-kaggle-ops-7/-8/-9, C1-validation-5 (docs part) | Docs pass: README Execution → kernel path; architecture guide Phase 3/4; runbook CURRENT STATE lines 10-14/36-46/81-88/101-127 rewritten, 129-162 superseded; known-defects status line per defect; spec ADOPTED RECIPE block; `docs/INDEX.md`; POLICY 5 lifted line; kernel table from `kernels_list`; quota ledger; sha256 + off-machine copy of the 11 checkpoints; distillation caveat restated next to every distilled OOF | `README.md`, `docs/*.md`, `kaggle/slotknee_models/` | no |
| 17 | C8-docs-and-process-8 | `kaggle/submit_common.py` shared by both submit kernels; 3 pytest cases with tiny checkpoints (two layouts → two groups; student excluded; CPU cap) | `kaggle/submit_kernel*/`, `tests/` | no |
| 18 | C6-rules-compliance-5/-6/-7 | Keep the GitHub repo private until after Oct 22; `check-visibility` (unauthenticated GET expecting 404) after every push; normalise `is_private` bools; post the host question on DUA-derived weights; mark KMAR-50K moot | `kaggle/kaggle_ops.py`, `kaggle/*/kernel-metadata.json`, runbook | no |
| 19 | C2-serve-skew-4/-6, C4-code-6/-7/-8, C3-labels-6 | Route only verified syntaxes through dicomsdl; `NO_ALBUMENTATIONS_UPDATE=1` + lazy imports + skip pip when importable; `group_map` from `n_labels`; per-fold `--limit-minutes` clock / `n_epochs_run` in npz; single checkpoint load; strip indication sections on any label re-extraction | `src/slots.py`, `src/slotknee.py`, `scripts/infer_slotknee.py`, `scripts/train_slotknee.py`, `kaggle/submit_kernel_eff/`, `api_labeler.py` | no |

### 4.2 Measured arms (need GPU), in priority order

| # | id(s) | arm | files | GPU |
|---|---|---|---|---|
| G1 | C1-validation-3, C7-experiment-design-1/-7 | `central_e16_s1337` on the t35 cache, folds 0-1 (~176 min): the 16-ep seed pair that fixes the gate, tests seed ensembling at 16 ep, spare member | `kaggle/ablate_kernel_g10/slotknee-ablate-g10.py` | T4 ~3 h |
| G2 | C2-serve-skew-1/-8, C4-code-5 | Rescore OOF: rows 6–9 zeroed / rows 0–5 resampled; t35 3-shift TTA on the OOF | `scripts/rescore_oof.py` | ~1 h Kaggle |
| G3 | C3-labels-1/-2 | `v4_majority` 2-fold arm at 16 ep on t35, gated on gold-58 + pilkwang + A/B-agree (not OOF-vs-v4); one LB probe if same-sign | ablate kernel, `data_subset/labels_external/v4_majority.csv` | T4 ~3 h + 1 submission |
| G4 | C7-experiment-design-2/-10 | `recipe_e24` on t35 (~265 min) — schedule length at zero inference cost | ablate kernel | T4 ~4.5 h |
| G5 | C7-experiment-design-2 | `zoomc` at 16 ep on t35 (~235 min; build the t35 zoomc cache on CPU first) — do the two adopts stack? | cache builder (CPU), ablate kernel | CPU ~40 min + T4 ~4 h |
| G6 | C1-validation-5 | `recipe_e16 --distill-weight 0` on folds 0-1 — bound the self-distillation inflation | ablate kernel | T4 ~3 h |
| G7 | C3-labels-3 | gold-weight 2 vs 8, 2-fold, gated per the label-side gate (or the contract change in 4.1 #11 instead) | ablate kernel, `src/llm_labels.py` | T4 ~5 h |
| G8 | C7-experiment-design-5 | Learning-curve arm (fold 0 at 50/75/100 % of its split, 16 ep) to price the full-fit gain | ablate kernel | T4 one session |
| G9 | C7-experiment-design-3 | At most one by prior: LoRA lr 5e-4 @ 16 ep (~5.6 h) or p336 @ 16 ep on t35 (~8 h) | ablate kernel | T4 6–8 h |
| G10 | C4-code-5 | Only if G2 moves beyond the gate: `group_embed[keep]` gather + 2-fold gate (then 8 h per 5-fold set) | `src/slotknee.py`, `scripts/train_slotknee.py` | T4 3 h (+ 8 h/set) |
| G11 | C1-validation-1 | Only if another gating campaign is planned: whole-tree fingerprint split (CPU kernel) + re-baseline the two 8-ep controls | new CPU kernel, `src/folds.py` | CPU + T4 ~3 h |
| G12 | runbook plan | v3-teacher student (1.6 h) and one full-fit member per band (2 × 1.6 h) — only after 4.1 #10 pre-registers the acceptance rule; both entries submitted once | `kaggle/train_kernel_student/` | T4 ~5 h + 2 submissions |

Suggested calendar (30 T4-h/week, 6 h reserve; GPU launches remain UI clicks): week of Sep 19 — G1, G4, G5,
G2 (fits in ~12 h); week of Sep 26 — G3, G6, and a 5-fold retrain (7 h) only for a combined, replicated
delta ≥ 0.005, else bank; week of Oct 3 — G8 and one of G9, then G12; week of Oct 10 — Oct 12 last cache
change, Oct 17 last recipe change; Oct 17–22 — final selection by the pre-registered rule on Oct 20, buffer.

---

## 5. Final-week checklist (Oct 15–22)

1. Working tree clean or committed; `tests/test_selection_metric.py` and the mixed-`is_gold` test green;
   `python3 -m pytest -q --ignore=tests/test_train_gpu_util.py` passes.
2. `slotknee-code` re-packaged from a clean tree with `MANIFEST.json`; version notes = git SHA; the runbook
   records code-dataset version, models-dataset version (file list) and image id for every scored kernel.
3. `slotknee-models` frozen: exactly the 11 (or final) checkpoints, no `stack_weights*.json` unless a scored
   submission adopted it; sha256 of every file recorded and an off-machine copy taken.
4. Both submit kernels pinned (`docker_image_pinning_type: original`), version banner and fatal decoder
   self-check in the log, failure-rate guard on, grouping key complete; re-run both kernels once on the
   frozen inputs and read the logs (two layout groups, student excluded, 0 failed, n_test printed).
5. Final two selections made by the pre-registered rule (slot 1: OOF-selected accuracy entry; slot 2:
   efficiency student); "Selected" check-marks confirmed in My Submissions and screenshotted; no probe
   submissions after selection; no API GPU pushes after selection.
6. `check-visibility` shows every kernel and both datasets private; GitHub repo stays private.
7. Quota ledger shows ≥ 6 h reserve for an emergency re-run; the CPU fallback tie-break and
   `CPU_TEST_STUDIES` fix are in the deployed kernel.
8. Winners' package draft exists (`docs/winners_package.md`: environment, reproduction order, licence table,
   paraphrased fixtures, `train_gold.csv` no longer shipped) so a Nov 5 request needs assembly, not
   discovery.
